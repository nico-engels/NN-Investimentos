<%- contentFor('body') %>
      <p class="nome_modulo">Bancos - Apagar</p>
      <form action="" method="POST">
        <table>
          <tr>
            <td class="form_label">ID:</td>
            <td><input class="form_dado_leitura" type="text" size="3" value="<%= banco.id %>" readonly /></td>
          </tr>
          <tr>
            <td class="form_label">Código:</td>
            <td><input class="form_dado_leitura" type="text" size="4" value="<%= banco.codigo %>" readonly /></td>
          </tr>
          <tr>
            <td class="form_label">Nome:</td>
            <td><input class="form_dado_leitura" type="text" size="50" value="<%= banco.nome %>" readonly /></td>
          </tr>
          <tr>
            <td class="form_label">CNPJ:</td>
            <td><input class="form_dado_leitura" type="text" size="19" value="<%= banco.cnpj %>" readonly /></td>
          </tr>
        </table>
        <p>Tem certeza que deseja excluir o registro?</p>
        <table>
          <tr>
            <td><a href="../editar/<%= banco.id %>"><button type="button">Voltar</button></a></td>
            <td><input value="Confirmar" type="submit" /></td>
          </tr>
        </table>
      </form>
      
